{
  "name": "zenbujs-demo",
  "version": "0.0.5",
  "private": true,
  "type": "module",
  "main": "node_modules/@zenbujs/core/dist/setup-gate.mjs",
  "scripts": {
    "dev": "zen dev",
    "build:source": "if [ -f .env.local ]; then set -a; . ./.env.local; set +a; fi && zen build:source",
    "build:electron": "if [ -f .env.local ]; then set -a; . ./.env.local; set +a; fi && zen build:electron",
    "publish:source": "if [ -f .env.local ]; then set -a; . ./.env.local; set +a; fi && zen publish:source",
    "link": "zen link",
    "db:generate": "zen db generate"
  },
  "dependencies": {
    "@tailwindcss/vite": "^4.2.0",
    "@vitejs/plugin-react": "^5.0.0",
    "@zenbujs/core": "^0.0.15",
    "lucide-react": "^1.14.0",
    "react": "^19.0.0",
    "react-dom": "^19.0.0",
    "tailwindcss": "^4.2.0",
    "vite": "^6.0.0",
    "zod": "^4.0.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "@types/react": "^19.0.0",
    "@types/react-dom": "^19.0.0",
    "electron": "^42.0.0",
    "electron-builder": "^26.0.0"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/zenbu-labs/zenbujs-demo.git"
  },
  "zenbu": {
    "host": ">=0.0.0 <0.1.0"
  },
  "pnpm": {
    "onlyBuiltDependencies": [
      "@parcel/watcher",
      "dugite",
      "electron",
      "electron-winstaller",
      "esbuild"
    ]
  }
}
