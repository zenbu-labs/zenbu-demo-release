# Copy to .env.local and fill in. Gitignored.
# Required for `pnpm build:electron` and `pnpm publish:source`.

# GitHub token with `repo` scope. `gh auth token` works.
GH_TOKEN=

# Apple notarization (only needed for `pnpm build:electron`).
APPLE_ID=
APPLE_APP_SPECIFIC_PASSWORD=
APPLE_TEAM_ID=
